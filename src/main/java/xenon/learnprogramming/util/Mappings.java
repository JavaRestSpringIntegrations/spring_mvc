package xenon.learnprogramming.util;

/*Notice the class name is plural, since this class will hold all the constants
Generally class names should be singular
final keyword is used for the class to make sure that it cannot be extended*/

public final class Mappings {

    // == constants ==
    public static final String ITEMS = "items";

    // constructor is private so it cannot be re-instantiated
    // == constrcutors ==
    private Mappings() {}

}
